{% extends 'base.html.twig' %}

{% block title %}Home Page{% endblock %}

{% block body %}
        <h1>Nos Produits  à la une !! <i class="fas fa-check"></i></h1>
        <div class="row">
            {% for p in product %}
            <div class="col">
               {% include 'product/_product_card.html.twig' %}
            </div>
            {% endfor %}
        </div>
        <hr>
        <h2>service super !!</h2>
        <p>Lorem ipsum dolor sit amet.</p>
        <div class="row">
            {% include 'shared/_assurance.html.twig' with{
                'color': 'primary',
                'icon': 'fas fa-truck',
                'text': "descrption un peu pourrie de mon assurance et des CGV"
            } %}
            {% include 'shared/_assurance.html.twig' with{
                'color': 'success',
                'icon': 'fas fa-check',
                'text': "descrption un peu pourrie de mon assurance et des CGV"
            } %}
            {% include 'shared/_assurance.html.twig' with{
                'color': 'warning',
                'icon': 'fas fa-lock',
                'text': "descrption un peu pourrie de mon assurance et des CGV"
            } %}
        </div>
        {#{% for c in  productRepository.findAll() %}
            <li>{{ c.name }}</li>
        {% endfor %}#}
{#<div class="container">
    <div id="carouselExampleFade" class="carousel slide carousel-fade" data-ride="carousel">
        <div class="carousel-inner">
            <div class="carousel-item active">
                <img src="https://picsum.photos/id/1059/700/400" class="d-block w-100" alt="...">
            </div>
            <div class="carousel-item">
                <img src="https://picsum.photos/id/1060/700/400" class="d-block w-100" alt="...">
            </div>
            <div class="carousel-item">
                <img src="https://picsum.photos/id/1068/700/400" class="d-block w-100" alt="...">
            </div>
        </div>
        
        <a class="carousel-control-prev" href="#carouselExampleFade" role="button" data-slide="prev">
            <span class="carousel-control-prev-icon" aria-hidden="true"></span>
            <span class="sr-only">Previous</span>
        </a>
        <a class="carousel-control-next" href="#carouselExampleFade" role="button" data-slide="next">
            <span class="carousel-control-next-icon" aria-hidden="true"></span>
            <span class="sr-only">Next</span>
        </a>
    </div>
</div>
   
    <h2>Découvrez le top 3 de nos ventes !!</h2>
<div class="container">
    <div class="card-group">
        {% for p in product  %}
        <div class="card">
            <img src="{{ p.picture }}" class="card-img-top" alt="...">
            <div class="card-body">
                <h5 class="card-title">{{ p.name }}</h5>
                <p class="card-text">{{ p.description }}</p>
                <p class="card-text"><small class="text-muted">{{ p.price }} euro</small></p>
            </div>
        </div>
    {% endfor %}
    </div>
</div>#}
{% endblock %}
