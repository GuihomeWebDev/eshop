{% extends 'base.html.twig' %}

{% block title %}Creation category{% endblock %}

{% block body %}
<h1> Creation d'une catégory</h1>
    {{ form_start(formView) }}
    {{ form_widget(formView) }}
    <button class="btn btn-primary">
        <i class="fas fa-save"></i>
        Creer la categorie</button>
    {{ form_end(formView) }}
{% endblock %}
