{% extends 'base.html.twig' %}

{% block title %}Modification de {{category.name}}{% endblock %}

{% block body %}
    <h1> Creation {{ category.name}}</h1>
    {{ form_start( formView ) }}
    {{ form_widget( formView ) }}
    <button class="btn btn-primary">
        <i class="fas fa-save"></i>
        Modifier le produit</button>
    {{ form_end( formView ) }}
{% endblock %}
