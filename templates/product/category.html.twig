{% extends 'base.html.twig' %}

{% block title %} category : {{ category.name }}{% endblock %}

{% block body %}
    <ol class="breadcrumb">
        <li class="breadcrumb-item"><a href="{{ path('home') }}">Acceuil</a></li>
        <li class="breadcrumb-item active">{{ category.name }}</li>
    </ol>
    <h1>page category: {{ category.name }}</h1>
        <div class="row">
            {% for p in category.product %}
                <div class="col-3">
                    {% include 'product/_product_card.html.twig' %}
                </div>
            {% endfor %}
        </div>

{% endblock %}